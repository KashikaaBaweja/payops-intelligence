from payops_core.eval.schema import EvalCase

CASES: list[EvalCase] = [
    EvalCase(
        id="DOC-01",
        category="documentation-only",
        question="What is the payment lifecycle?",
        expected_behavior="Retrieve lifecycle docs only; skip SQL and webhooks; complete with citations.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["payment-lifecycle"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="DOC-02",
        category="documentation-only",
        question="What does GATEWAY_TIMEOUT mean?",
        expected_behavior="Retrieve error-code/runbook docs; do not query payments SQL.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["error-codes", "payment-failures", "incident-runbook"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="DOC-03",
        category="documentation-only",
        question="Summarize the refund policy.",
        expected_behavior="Retrieve refund policy documentation only.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["refund-policy"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="DOC-04",
        category="documentation-only",
        question="How should disputes be interpreted in the ops documentation?",
        expected_behavior="Retrieve dispute docs; do not treat disputes as a capture outage.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["disputes-guide"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="DOC-05",
        category="documentation-only",
        question="What are the incident runbook steps in the documentation?",
        expected_behavior="Retrieve the incident runbook; no metrics query required.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["incident-runbook"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="DOC-06",
        category="documentation-only",
        question="What does AUTHENTICATION_FAILED mean?",
        expected_behavior="Retrieve the error-code catalog for AUTHENTICATION_FAILED.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        relevant_doc_ids=["error-codes"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="SQL-01",
        category="sql-required",
        question="What is the payment success rate for M102?",
        expected_behavior="Call the metrics catalog; do not require webhook inspection.",
        expected_tools=["query_metrics"],
        forbidden_tools=["inspect_webhooks"],
        required_sources=["metric"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="SQL-02",
        category="sql-required",
        question="What is the payment failure rate for M102?",
        expected_behavior="Query failure_rate from the SQL gateway for Harbor Retail.",
        expected_tools=["query_metrics"],
        forbidden_tools=["inspect_webhooks"],
        required_sources=["metric"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="SQL-03",
        category="sql-required",
        question="What is the refund rate for M102?",
        expected_behavior="Query refund metrics; refund policy docs may also be retrieved.",
        expected_tools=["query_metrics"],
        required_sources=["metric"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="SQL-04",
        category="sql-required",
        question="What is the error code breakdown for M102 failures?",
        expected_behavior="Query error-code breakdown (and docs if the planner also retrieves).",
        expected_tools=["query_metrics"],
        required_sources=["metric"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="SQL-05",
        category="sql-required",
        question="What is the payment success rate for M201?",
        expected_behavior="Query success_rate for Cedar Digital Goods.",
        expected_tools=["query_metrics"],
        forbidden_tools=["inspect_webhooks"],
        required_sources=["metric"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="WH-01",
        category="webhook-required",
        question="Find delayed webhook events for M201",
        expected_behavior="Inspect webhook delays for Cedar Digital Goods; complete with webhook evidence.",
        expected_tools=["inspect_webhooks"],
        forbidden_tools=["query_metrics"],
        required_sources=["webhook"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="WH-02",
        category="webhook-required",
        question="Find webhook retries for M201",
        expected_behavior="Use the webhook inspector for retries, not raw SQL.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=False,
    ),
    EvalCase(
        id="WH-03",
        category="webhook-required",
        question="Find duplicate webhook events for Cedar Digital Goods M201",
        expected_behavior="Inspect duplicates via the webhook catalog.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=False,
    ),
    EvalCase(
        id="WH-04",
        category="webhook-required",
        question="Inspect delayed ACK consumers for M201",
        expected_behavior="Treat delayed ACK as a webhook investigation, not a decline query.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="MULTI-01",
        category="multi-source",
        question="Why did Harbor Retail M102 UPI payments fail with GATEWAY_TIMEOUT?",
        expected_behavior="Plan docs + metrics, refine until both sources exist, then verify and write.",
        expected_tools=["retrieve_docs", "query_metrics"],
        relevant_doc_ids=["payment-failures", "incident-runbook", "error-codes"],
        required_sources=["doc", "metric"],
        expected_sufficient=True,
        expect_refine=True,
    ),
    EvalCase(
        id="MULTI-02",
        category="multi-source",
        question="Merchant health scorecard for Harbor Retail M102",
        expected_behavior="Run the merchant health tool for M102.",
        expected_tools=["merchant_health"],
        required_sources=["health"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="MULTI-03",
        category="multi-source",
        question="Harbor Retail M102 GATEWAY_TIMEOUT runbook and success rate",
        expected_behavior="Combine runbook retrieval with a success-rate catalog query.",
        expected_tools=["retrieve_docs", "query_metrics"],
        relevant_doc_ids=["incident-runbook", "payment-failures"],
        required_sources=["doc", "metric"],
        expected_sufficient=True,
        expect_refine=True,
    ),
    EvalCase(
        id="MULTI-04",
        category="multi-source",
        question="Cedar M201 delayed webhooks and success rate",
        expected_behavior="Inspect webhooks and query success rate for the same merchant.",
        expected_tools=["inspect_webhooks", "query_metrics"],
        required_sources=["webhook", "metric"],
        expected_sufficient=True,
        expect_refine=True,
    ),
    EvalCase(
        id="INSUF-01",
        category="insufficient-evidence",
        question="Find delayed webhook events for M999",
        expected_behavior="Stop at the iteration cap with evidence_sufficient false; do not invent a cause.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=False,
        expect_refine=False,
        max_iterations=1,
    ),
    EvalCase(
        id="INSUF-02",
        category="insufficient-evidence",
        question="Why did Harbor Retail M102 UPI payments fail with GATEWAY_TIMEOUT?",
        expected_behavior="With max_iterations=1, exit to the writer before both evidence categories arrive.",
        expected_tools=["retrieve_docs", "query_metrics"],
        expected_sufficient=False,
        expect_refine=False,
        max_iterations=1,
    ),
    EvalCase(
        id="INSUF-03",
        category="insufficient-evidence",
        question="Find delayed webhook events for M000",
        expected_behavior="Unknown merchant webhooks yield an incomplete report, not a guessed outage.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=False,
        max_iterations=1,
    ),
    EvalCase(
        id="AMB-01",
        category="ambiguous-question",
        question="What happened in the payments documentation?",
        expected_behavior="Fall back to documentation retrieval; still produce a structured report.",
        expected_tools=["retrieve_docs"],
        relevant_doc_ids=["payment-lifecycle", "payment-failures"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="AMB-02",
        category="ambiguous-question",
        question="Investigate the issue",
        expected_behavior="Planner still emits at least one retrieve_docs task and the graph reaches the writer.",
        expected_tools=["retrieve_docs"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="AMB-03",
        category="ambiguous-question",
        question="Is the platform okay according to the docs?",
        expected_behavior="Treat as a docs question; do not require SQL or webhooks.",
        expected_tools=["retrieve_docs"],
        forbidden_tools=["query_metrics", "inspect_webhooks"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="CONF-01",
        category="conflicting-evidence",
        question="What does GATEWAY_TIMEOUT mean?",
        expected_behavior="Reject an injected unsupported cause; do not promote it in the final report.",
        expected_tools=["retrieve_docs"],
        relevant_doc_ids=["error-codes", "payment-failures"],
        required_sources=["doc"],
        expected_sufficient=False,
        expect_unsupported=True,
        inject_hypothesis={
            "cause": "The outage was caused by a lunar radiation event",
            "confidence": 0.95,
            "category": "processor",
        },
    ),
    EvalCase(
        id="CONF-02",
        category="conflicting-evidence",
        question="What does GATEWAY_TIMEOUT mean?",
        expected_behavior="Reject an injected unsupported issuer cause against GATEWAY_TIMEOUT evidence.",
        expected_tools=["retrieve_docs"],
        required_sources=["doc"],
        expected_sufficient=False,
        expect_unsupported=True,
        inject_hypothesis={
            "cause": "The decline was caused by a solar-flare packet flood",
            "confidence": 0.95,
            "category": "issuer",
        },
    ),
    EvalCase(
        id="ANOM-01",
        category="anomaly-investigation",
        question="Why did Harbor Retail M102 UPI GATEWAY_TIMEOUT spike at midday?",
        expected_behavior="Investigate the planted UPI processor incident with docs and metrics.",
        expected_tools=["retrieve_docs", "query_metrics"],
        relevant_doc_ids=["payment-failures", "incident-runbook"],
        required_sources=["doc", "metric"],
        expected_sufficient=True,
        expect_refine=True,
    ),
    EvalCase(
        id="ANOM-02",
        category="anomaly-investigation",
        question="Find delayed webhook events for Cedar Digital Goods M201",
        expected_behavior="Surface the planted webhook-delay incident for M201.",
        expected_tools=["inspect_webhooks"],
        expected_sufficient=True,
    ),
    EvalCase(
        id="ANOM-03",
        category="anomaly-investigation",
        question="Should ops name a root cause for Low-volume Labs M305 from the runbook?",
        expected_behavior="Retrieve the sparse-volume runbook guidance; do not force a SQL incident narrative.",
        expected_tools=["retrieve_docs"],
        relevant_doc_ids=["incident-runbook"],
        required_sources=["doc"],
        expected_sufficient=True,
    ),
]


def all_categories() -> set[str]:
    return {case.category for case in CASES}
